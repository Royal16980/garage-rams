[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "garage-rams"
version = "0.1.0"
description = "Risk Assessment App"
readme = "README.md"
authors = [
    {name = "Garage RAMS Team"}
]
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = []

[project.scripts]
"garage-rams" = "garage_rams.ui:main"
